import { SsnInputDirective } from './ssn-input.directive';

describe('SsnInputDirective', () => {
  it('should create an instance', () => {
    const directive = new SsnInputDirective();
    expect(directive).toBeTruthy();
  });
});
